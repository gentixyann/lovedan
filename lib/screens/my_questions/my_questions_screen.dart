import 'package:flutter/material.dart';

class MyQuestionsScreen extends StatelessWidget {
  static const routeName = '/myQuestions';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('MyQuestionsScreen'),
      ),
    );
  }
}
